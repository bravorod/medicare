# reports/memo

`memo.md` is rendered by `partd-risk results` from `memo_template.md.j2` and
`reports/results/headline_metrics.json`. Edit the template, not the rendered
memo. Every number comes from the metrics file, and every claim that depends
on the direction of a result is conditional in the template.
